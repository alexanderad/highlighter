version: "2"
services:
  redis:
    image: redis
    ports:
      - 6379:6379
    volumes:
      - ./data:/data
  app:
    image: alexanderad/highlighter:latest
    environment:
      - PRODUCTION=1
    ports:
      - 8000:8000
    depends_on:
      - redis
